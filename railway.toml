[build]
builder = "nixpacks"

[deploy]
startCommand = "mkdir -p /tmp/matplotlib /tmp/fontconfig-cache && python -u bot.py"
numReplicas = 1
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
